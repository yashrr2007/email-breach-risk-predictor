{
  "name": "email-breach-risk-api",
  "version": "1.0.0",
  "description": "Backend API that detects if an email was exposed in known breaches and predicts account compromise risk with an AI risk engine.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "tunnel": "tools\\cloudflared.exe tunnel --url http://localhost:3000",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
